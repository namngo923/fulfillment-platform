namespace FulfillmentPlatform.Tests.Integration.Inventory;

public static class ConcurrentReservationTests
{
    public const string Scenario = "Validate no oversell when multiple reservations hit the same stock bucket.";
}
