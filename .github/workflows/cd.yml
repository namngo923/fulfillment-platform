name: cd

on:
  workflow_dispatch:

jobs:
  deploy-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deployment note
        run: echo "Add ECS or container deployment pipeline when runtime infrastructure is ready."
