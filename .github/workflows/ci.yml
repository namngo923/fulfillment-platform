name: ci

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
      - name: Restore
        run: dotnet restore FulfillmentPlatform.sln
      - name: Build
        run: dotnet build FulfillmentPlatform.sln --configuration Release --no-restore
      - name: Test
        run: dotnet test FulfillmentPlatform.sln --configuration Release --no-build --verbosity normal


